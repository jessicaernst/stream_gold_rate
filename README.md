# stream_gold_rate

## Aufgabenstellung:

Die App besteht aus nur einem Screen und soll in Echtzeit den aktuellen Goldkurs anzeigen.

Eine Fake Gold API mit der Funktion getGoldPriceStream existiert schon im Projekt.

Implementiere im bestehenden UI einen StreamBuilder und reagiere auf die 3 Zustände:

Ladend
Daten (Goldpreis vorhanden)
Error

### hinweis:
der vierte Fall ist Automatismus sobald ich einen StreamBuilder scheibe, hier kann der nicht eintreten, bei einem simulierten Stream, ja gut. Kann man streiten, kann man auch sein lassen, fiel mir auf da war er schon mehr als halb da. Alos habe ich ihn gelassen

